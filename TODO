Wrap up all the missing changes.
    Rename operations.
    Views.
    Trigger.

Finish POD.

Figure out the bootstrapping system.  Users need a script and a way to add
monorail to an existing project.

Refactor the StandardSQL change role to get as_sql into the role, define
producer method name and as_sql_trans_obj() in the class.

Fix autogened subs to have names.
