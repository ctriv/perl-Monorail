Wrap up all the missing changes.

Finish tests.

Finish POD.

Autodetect db type.

We're calling the DBIx::Class bits dbix, schema and model in different places,
pick one.

Figureout the bootstrapping system.  Users need a script and a way to add monorail to an existing project.

Refactor the StandardSQL change role to get as_sql into the role, define producer method name and as_sql_trans_obj() in the class.

Fix autogened subs to have names.
